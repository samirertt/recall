# Engineering Memory — Frontend

See the [repo root README](../README.md) for what this is, how to run it (backend +
frontend together), and the project layout. This package is a Vite + React 19 +
TypeScript + Tailwind CSS 4 app — `npm install && npm run dev`.
